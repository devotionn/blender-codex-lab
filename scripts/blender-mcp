#!/bin/sh
set -eu
PROJECT_ROOT=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
export BLENDER_MCP_HOST=127.0.0.1
export BLENDER_MCP_PORT=9876
exec "$PROJECT_ROOT/.venv/bin/python" -m blmcp --transport stdio
